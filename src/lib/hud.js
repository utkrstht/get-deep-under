import { k } from "../main.js";
import { gameState } from "../store.js";

function depth() {
    const depthLabel = k.add([
    ]);

    k.onUpdate(() => {
    });
}

export function loadHUD() {
    depth();
}
